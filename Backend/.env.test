NODE_ENV=test
PORT=3001

# Database Configuration (Docker container - CORRECT PASSWORD)
DB_HOST=localhost
DB_PORT=5433
DB_NAME=coachgpt_test
DB_USER=postgres
DB_PASSWORD=password

# JWT Configuration
JWT_SECRET=test-jwt-secret-key-for-testing-only
JWT_EXPIRES_IN=1h

# Test-specific settings
BCRYPT_ROUNDS=4
LOG_LEVEL=error